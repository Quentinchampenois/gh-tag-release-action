# action.yml
name: 'Release tags from label'
description: 'Define the next release tag using label'
inputs:
  crash_on_error:
    description: 'If set to "true", the action will crash if an error occurs'
    required: false
    default: "true"
outputs:
  tag:
    description: 'The new release tag version'
runs:
  using: 'docker'
  image: 'Dockerfile'
